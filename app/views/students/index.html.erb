<h1>MDL 252</h1>
<div class="student-table">
	<% @row_counter = 0 %>
	<% @students.each do |student| %>
		<% if @row_counter == 0 %>
			<div class="row">
		<% end %>
		<% @row_counter+=1 %>
		<div class="two columns profile">
			<div>
				<a href='#' class="toggle" gumby-trigger="#<%=student.last_name%>infodrawer.drawer"><%=picture(student)%></a>
			</div>
		</div>
		<% if @row_counter == 6 %>
			</div>
			<% @row_counter = 0 %>
		<% end %>
	<% end %>
</div>

<p style="margin-top: 50px;">Sort by:</p>

<div class="sorting-buttons">
	<div class="medium primary btn">
	  <%= link_to "A <i class='icon-right-thin'></i> Z".html_safe, sort: "last_name" %>
	</div>
	<div class="medium primary btn">
	  <%= link_to "ICM", sort: "icm, last_name" %>
	</div>
	<div class="medium primary btn">
	  <%= link_to "Top Gunner", sort: "score, last_name" %>
	</div>
</div>

